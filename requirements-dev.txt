# Dependencies for running the analysis notebook (seismic_bumps_analysis.ipynb).
# Not needed to run the Streamlit demo (see requirements.txt). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

xgboost==2.0.3
matplotlib>=3.7
scipy>=1.11

# Notebook execution
jupyter>=1.0
nbformat>=5.9
nbconvert>=7.0
ipykernel>=6.0
